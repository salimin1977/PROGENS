name: PROGENS Build Validation
on:
  push:
    branches: ['main', 'phase-2-data-engine', 'phase-3-supabase-provider', 'feat/phase4-complete', 'feat/phase5-final', 'feat/phases6-8-complete', 'phase9-10']
  pull_request:
    branches: ['main', 'phase-3-supabase-provider', 'feat/phase4-complete', 'feat/phase5-final', 'feat/phases6-8-complete', 'phase9-10']
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm install
      - run: npm run build
      - run: npm run lint
